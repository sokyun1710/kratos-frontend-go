apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.26.0 (40646f47)
  creationTimestamp: null
  labels:
    io.kompose.service: opa
  name: opa
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: opa
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert
        kompose.version: 1.26.0 (40646f47)
      creationTimestamp: null
      labels:
        io.kompose.network/intranet: "true"
        io.kompose.service: opa
    spec:
      containers:
        - args:
            - run
            - --server
            - --log-format=json-pretty
            - --set=decision_logs.console=true
            - --log-level=debug
          image: openpolicyagent/opa:latest
          name: opa
          ports:
            - containerPort: 8181
          resources: {}
          volumeMounts:
            - mountPath: /policies
              name: opa-claim0
      restartPolicy: Always
      volumes:
        - name: opa-claim0
          persistentVolumeClaim:
            claimName: opa-claim0
status: {}
